version: 2
mergeable:
  - when: pull_request.*
    validate:
      # validate title
      - do: description
        and:
          - must_exlude:
              regex: 'must_exclude'
          - must_include:
              regex: 'must_include'